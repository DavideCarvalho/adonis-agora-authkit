---
"@adonis-agora/authkit-server": patch
"@adonis-agora/authkit-react": patch
"@adonis-agora/authkit-client": patch
---

Ship TanStack Intent AI-agent skills with each package (`skills/` added to `files`), add the `tanstack-intent` keyword, and take a devDependency on `@tanstack/intent` for skill validation.
